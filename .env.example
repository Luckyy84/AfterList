# TMDB movie/TV search
# Use one of these values for local development.
# Vite exposes VITE_* values to the browser, so do not use private production secrets here.

VITE_TMDB_API_KEY=
VITE_TMDB_ACCESS_TOKEN=
